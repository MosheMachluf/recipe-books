export default {
  allBooks(state) {
    return state.allBooks;
  },

  myBooks(state) {
    return state.myBooks;
  },

  bookRecipes(state) {
    return state.bookRecipes;
  },

  currentBook(state) {
    return state.currentBook;
  },
};
